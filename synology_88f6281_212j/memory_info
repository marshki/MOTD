#!/usr/bin/env bash 

memory() {
  mems=$(awk '/MemTotal:/ { sub(/^.*: /, ""); print; }' /proc/meminfo)
  printf "%s\n" "$mems"  
} 

memory 

# awk '/MemTotal:/ { sub(/^.*: /, ""); print; }' /proc/meminfo

# 930384 kB
